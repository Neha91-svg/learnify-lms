# Set default behavior to normalize line endings
* text=auto

# Force specific file types to use LF (good for scripts, JS, etc.)
*.js text eol=lf
*.ts text eol=lf
*.json text eol=lf
*.html text eol=lf
*.css text eol=lf

# Force Windows line endings (CRLF) for batch files
*.bat text eol=crlf
